# requirements.txt - 해외주식 자동매도 시스템 (기획서 v2.0 & 실전 매매 통합)
# 작성일: 2025-10-21
# Python 3.8 이상 필요

# ================================
# 1. 핵심 의존성 (System Core)
# ================================

# HTTP 클라이언트 (한국투자증권 API 호출)
requests~=2.31.0

# WebSocket 클라이언트 (실시간 체결 데이터)
websocket-client~=1.8.0

# YAML 파서 & 환경 변수 (설정 로드)
PyYAML~=6.0.1
python-dotenv~=1.0.0

# 시간대 처리 (미국 동부시간 계산)
pytz~=2024.1

# ================================
# 2. 데이터 분석 (Data Science)
# ================================
# 전략 계산(EMA) 및 캔들 데이터 처리에 필수
pandas~=2.2.0
numpy>=1.24.0

# ================================
# 3. 보안 관련 (Security)
# ================================

# HTTP 라이브러리 보안 패치
urllib3~=2.1.0

# SSL 인증서 (HTTPS 통신)
certifi>=2023.11.17

# ================================
# 4. 선택적 의존성 (Optional)
# ================================

# 웹 대시보드 (필요 시에만 설치)
flask~=3.0.0
werkzeug~=3.0.1

# ================================
# 설치 가이드
# ================================
# pip install -r requirements.txt